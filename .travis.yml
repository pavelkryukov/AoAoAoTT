notifications:
  email: false

language: c++
sudo: false

dist: xenial

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - libboost-dev
    - libstdc++-7-dev
    - g++-7

branches:
  only:
  - master
  
compiler:
  - g++-7

script:
  - if [ $CXX == g++ ] && [ "$TRAVIS_OS_NAME" == "linux" ]; then CXX=g++-7; fi
  - git clone https://github.com/boostorg/pfr
  - cd test
  - make CXXFLAGS='-fprofile-arcs -ftest-coverage -g' LDFLAGS='-lgcov --coverage' BOOST_PFR_DIR=../pfr/include test
  - ./test

after_success:
  - bash <(curl -s https://codecov.io/bash) || echo "Codecov did not collect coverage reports"
